# 0x00. C - Hello, World

| Tasks | Description|
|---|---|
| 0-preprocessor | Run c preprocessing code and put result into a file |
| 1-compiler | a script that compiles a C file but does not link it |
| 100-intel | a script that generates the assembly code (Intel syntax) of a C code|
| 101-quote.c | a script that print a text in c without using printf or puts |
| 2-assembler | a script that generates the assembly code of a C code |
| 3-name | a script that compiles a C file and creates an executable named x |
| 4-puts.c | C program that prints a string | 
| 5-printf.c | C program that prints exactly with proper grammar |
| 6-size.c | C program that prints the size of various types on the computer |
